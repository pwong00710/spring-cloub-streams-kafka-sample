spring:
  profiles:
    active: test
  application:
    name: consumer-service
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false      
